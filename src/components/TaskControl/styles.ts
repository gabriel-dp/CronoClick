"use client";

import styled from "styled-components";

export const ControlBar = styled.div`
	width: min(60rem, 100%);
	padding: 1rem;
	margin: auto;

	display: flex;
	justify-content: space-around;
`;
